import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreespaacesolution.ssscore',
  appName: 'SSS Core App',
  webDir: 'dist'
};

export default config;
